export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-surface">
      <p className="font-sans text-text-muted">Home — browse & discover</p>
    </main>
  );
}
